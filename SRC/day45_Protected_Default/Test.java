package day45_Protected_Default;

import day45_Inheritance.Protected_VS_Default;

public class Test {
	
	Protected_VS_Default obj = new Protected_VS_Default();
	// obj.printHello(); this is protected method, cannot be visible outside the package
	// obj.printHola(); this is protected method, cannot be visible outside the package

}
